import React from "react";

export default function Text({ text, style }) {
  return <p style={style}>{text}</p>;
}

import React from "react";

export default function Text({ text, style }) {
  return <p style={style}>{text}</p>;
}
