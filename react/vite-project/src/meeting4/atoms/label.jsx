import React from "react";

export default function Label({ text, style }) {
  return <label style={style}>{text}</label>;
}

import React from "react";

export default function Label({ text, style }) {
  return <label style={style}>{text}</label>;
}
